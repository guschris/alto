{
  "openai": {
    "apiKey": "YOUR_API_KEY",
    "baseUrl": "http://localhost:8080/v1",
    "model": "qwen/qwen3-30b-a3b"
  },
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "list_files",
        "description": "List files/directories in specified path. `recursive: true` for recursive, `false`/omit for top-level only. Do not use to confirm created files; user will confirm.",
        "parameters": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Directory path (relative to /home/chris/alto)"
            },
            "recursive": {
              "type": "boolean",
              "description": "True for recursive, false/omit for top-level."
            }
          },
          "required": ["path"]
        }
      }
    }
  ]
}
